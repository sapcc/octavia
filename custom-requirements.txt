# Any requirements we need for CCloud

# tracing
osprofiler>=1.4.0 # Apache-2.0
jaeger-client>=4.1.0 # loci build wants 4.3.0

# uwsgi plugins
uwsgi-dogstatsd
uwsgi-shortmsecs

raven
-e git+https://github.com/sapcc/octavia-f5-provider-driver.git@stable/yoga-m3#egg=octavia-f5-provider-driver
git+https://github.com/sapcc/openstack-watcher-middleware.git#egg=watcher-middleware
git+https://github.com/sapcc/openstack-audit-middleware.git@master#egg=audit-middleware
git+https://github.com/sapcc/openstack-uwsgi-middleware.git@main#egg=uwsgi-middleware

# This was oslo.db[mariadb]>=8.4.0 in requirements.txt, but that doesn't work
# with custom oslo.db # repo in upper-constraints.txt, so we specify that
# custom repo again here. This can probably be removed again once Octavia is
# upgraded. See also test-requirements.txt
-e git+https://github.com/sapcc/oslo.db.git@stable/yoga-m3#egg=oslo.db
